# Sage — dependencies
# Core
openai>=1.0.0           # Qwen Cloud (OpenAI-compatible)
json5>=0.9.0            # Flexible JSON parsing
pydantic>=2.0           # Data validation and models
httpx>=0.27.0           # Async HTTP client with connection pooling
numpy>=1.24.0           # Vector math for embedding store (cosine similarity)
fastapi>=0.111.0        # Modern API backend
uvicorn>=0.30.0         # ASGI server for FastAPI

# Alibaba Cloud MCP (for deployment)
# Will be added when integration starts

# Development
pytest>=7.0.0
pytest-cov>=5.0         # Coverage measurement
